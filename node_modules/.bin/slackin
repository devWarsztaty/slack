#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../slackin/bin/slackin" "$@"
  ret=$?
else 
  node  "$basedir/../slackin/bin/slackin" "$@"
  ret=$?
fi
exit $ret
